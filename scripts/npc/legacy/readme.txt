All npcs here are odin based npcs
They will be loaded if there is no script for them in the main 'npc' folder.
They aren't guaranteed to work 100% but as long as they only use 'cm' methods it should work
All odin scripts will also support java 7, main scripts must use java 8