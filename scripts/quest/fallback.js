
function start(mode, type, selection){

	qm.sendMessage("PINK_TEXT", qm.getQuest().getId()+" has no script!");
	qm.dispose();
	
}

function end(mode, type, selection){
	qm.sendMessage("PINK_TEXT", qm.getQuest().getId()+" has no script!");
	qm.dispose();
}